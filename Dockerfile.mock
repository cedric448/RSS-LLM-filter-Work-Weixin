FROM python:3.11-slim

WORKDIR /app

# 安装依赖
RUN pip install --no-cache-dir flask

# 复制应用代码
COPY mock-rss-server.py /app/

# 创建配置目录
RUN mkdir -p /app/config /app/logs

# 暴露端口
EXPOSE 5000

# 启动命令
CMD ["python", "mock-rss-server.py"]
