# 配置说明文档

## 1. 配置文件概览

| 文件路径 | 用途 | 格式 | 必需 |
|---------|------|------|------|
| `config/rss-sources.json` | RSS 源配置 | JSON | ✅ |
| `config/keywords.json` | AI 筛选规则 | JSON | ✅ |
| `docker-compose.yml` | Docker 服务编排 | YAML | ❌ (备用) |
| `src/auto_push.py` | 运行时参数 | Python | ✅ |
| `src/llm_filter.py` | AI API 配置 | Python | ✅ |
| `src/wechat_pusher.py` | 推送配置 | Python | ✅ |

---

## 2. config/rss-sources.json

### 2.1 配置格式

```json
{
  "sources": [
    {
      "name": "AI前线",
      "slug": "ai-frontiers",
      "url": "https://www.jintiankansha.me/column/ai-frontiers",
      "enabled": true
    }
  ]
}
```

### 2.2 字段说明

| 字段 | 类型 | 必需 | 说明 | 示例 |
|------|------|------|------|------|
| `name` | string | ✅ | RSS 源名称,显示在推送消息中 | "AI前线" |
| `slug` | string | ✅ | 今天看啥的公众号 slug,用于 API4 | "ai-frontiers" |
| `url` | string | ❌ | RSS 订阅链接 (RSS模式使用) | "https://..." |
| `enabled` | boolean | ✅ | 是否启用该源 | true/false |

### 2.3 获取 slug 的方法

#### 方法 1: 从今天看啥网站获取

1. 登录 https://www.jintiankansha.me
2. 进入专栏页面
3. URL 中 `/column/{slug}` 即为 slug

**示例**:
```
URL: https://www.jintiankansha.me/column/ai-frontiers
Slug: ai-frontiers
```

#### 方法 2: 使用测试脚本

```bash
# 测试单个 slug
python3 << EOF
import requests

API4_USER = "your_email@example.com"
API4_TOKEN = "your_token"
slug = "ai-frontiers"

url = f"http://www.jintiankansha.me/api3/query/get_topics_by_one_column"
params = {"user": API4_USER, "token": API4_TOKEN, "slug": slug}

response = requests.get(url, params=params)
print(f"状态码: {response.status_code}")
print(f"响应: {response.json()}")
EOF
```

### 2.4 配置示例

#### 单个源配置

```json
{
  "sources": [
    {
      "name": "AI前线",
      "slug": "ai-frontiers",
      "url": "https://www.jintiankansha.me/column/ai-frontiers",
      "enabled": true
    }
  ]
}
```

#### 多源配置

```json
{
  "sources": [
    {
      "name": "AI前线",
      "slug": "ai-frontiers",
      "url": "https://www.jintiankansha.me/column/ai-frontiers",
      "enabled": true
    },
    {
      "name": "投资界",
      "slug": "investment-circle",
      "url": "https://www.jintiankansha.me/column/investment-circle",
      "enabled": true
    },
    {
      "name": "36氪",
      "slug": "36kr",
      "url": "https://www.jintiankansha.me/column/36kr",
      "enabled": false
    }
  ]
}
```

### 2.5 注意事项

1. **slug 必须正确**: 错误的 slug 会导致 API 返回 404
2. **enabled 控制**: 临时禁用某个源时,将 `enabled` 设为 `false`
3. **源数量限制**: 建议 < 200 个源 (今天看啥 VIP-6 套餐限制)
4. **名称唯一性**: 建议不同源使用不同名称,避免混淆

---

## 3. config/keywords.json

### 3.1 配置格式

```json
{
  "categories": [
    {
      "name": "AIGC",
      "keywords": ["生成式AI", "AIGC", "内容生成", "文生图", "文生视频"],
      "description": "生成式人工智能内容"
    }
  ],
  "match_mode": "semantic",
  "threshold": 0.7
}
```

### 3.2 字段说明

#### categories (数组)

| 字段 | 类型 | 必需 | 说明 | 示例 |
|------|------|------|------|------|
| `name` | string | ✅ | 分类名称,显示在推送消息中 | "AIGC" |
| `keywords` | array | ✅ | 关键词列表,用于语义匹配 | ["生成式AI", "AIGC"] |
| `description` | string | ❌ | 分类描述,用于构建 AI 提示词 | "生成式人工智能内容" |

#### 全局配置

| 字段 | 类型 | 必需 | 说明 | 默认值 |
|------|------|------|------|--------|
| `match_mode` | string | ❌ | 匹配模式: `semantic`(语义) 或 `keyword`(精确) | "semantic" |
| `threshold` | float | ❌ | 置信度阈值 (0.0-1.0) | 0.7 |

### 3.3 预设分类示例

```json
{
  "categories": [
    {
      "name": "AIGC",
      "keywords": [
        "生成式AI", "AIGC", "内容生成",
        "文生图", "Midjourney", "Stable Diffusion",
        "文生视频", "Sora", "Runway",
        "AI绘画", "AI作曲", "AI写作"
      ],
      "description": "生成式人工智能内容创作"
    },
    {
      "name": "AI初创",
      "keywords": [
        "AI初创", "AI公司", "AI创业", "AI企业",
        "AI团队", "AI实验室", "AI startup"
      ],
      "description": "AI初创公司和团队动态"
    },
    {
      "name": "AI融资",
      "keywords": [
        "AI融资", "AI投资", "AI估值", "AI IPO",
        "AI并购", "AI收购", "天使轮", "A轮", "B轮",
        "独角兽", "VC", "PE"
      ],
      "description": "AI领域投融资新闻"
    },
    {
      "name": "大模型",
      "keywords": [
        "大模型", "LLM", "大语言模型",
        "GPT", "Claude", "Gemini", "Llama",
        "Transformer", "预训练", "微调"
      ],
      "description": "大语言模型技术进展"
    },
    {
      "name": "AI产品",
      "keywords": [
        "AI产品", "AI应用", "AI工具", "AI助手",
        "AI Agent", "Copilot", "AI插件",
        "AI功能", "AI集成"
      ],
      "description": "AI产品发布和功能更新"
    },
    {
      "name": "AI行业",
      "keywords": [
        "AI趋势", "AI报告", "AI白皮书",
        "AI政策", "AI监管", "AI伦理",
        "AI市场", "AI竞争", "AI格局"
      ],
      "description": "人工智能行业动态和趋势"
    }
  ],
  "match_mode": "semantic",
  "threshold": 0.7
}
```

### 3.4 配置建议

#### 3.4.1 关键词设计原则

1. **语义丰富**: 包含同义词、相关词、英文缩写
2. **覆盖全面**: 考虑不同表达方式
3. **避免过泛**: 不要使用"技术"、"新闻"等通用词

**好的示例**:
```json
["生成式AI", "AIGC", "内容生成", "文生图", "Midjourney"]
```

**不好的示例**:
```json
["AI", "技术", "创新"]  // 太泛
```

#### 3.4.2 置信度阈值调整

| 阈值 | 效果 | 适用场景 |
|------|------|---------|
| 0.5 | 宽松,更多文章通过 | 初期测试,广撒网 |
| 0.7 | 推荐,平衡精准度和召回率 | 生产环境 |
| 0.9 | 严格,只推送高度相关文章 | 精准推送 |

**调整示例**:
```json
{
  "threshold": 0.8  // 提高到 0.8,更严格筛选
}
```

#### 3.4.3 分类数量建议

- **最少**: 3-5 个核心分类
- **推荐**: 5-10 个分类
- **最多**: 不超过 15 个 (过多会影响 AI 判断精度)

### 3.5 动态更新

配置文件支持热更新,无需重启服务:

```bash
# 编辑配置
vim config/keywords.json

# 下次执行时自动生效
# (每次推送时重新读取配置)
```

### 3.6 测试筛选规则

创建测试脚本 `scripts/test_filter.py`:

```python
#!/usr/bin/env python3
import sys
sys.path.insert(0, '/root/project-wb/n8n/src')

from llm_filter import filter_articles_batch

# 测试文章
test_articles = [
    {
        'title': 'OpenAI 发布 GPT-5,支持超长上下文',
        'summary': 'OpenAI 今日宣布推出 GPT-5 大模型...',
        'source_name': '测试源',
        'link': 'https://example.com/1'
    },
    {
        'title': '苹果发布新款 iPhone',
        'summary': '苹果公司发布了新款 iPhone 15...',
        'source_name': '测试源',
        'link': 'https://example.com/2'
    }
]

# 执行筛选
filtered = filter_articles_batch(test_articles)

print(f"输入: {len(test_articles)} 篇")
print(f"输出: {len(filtered)} 篇\n")

for article in filtered:
    print(f"标题: {article['title']}")
    print(f"分类: {', '.join(article.get('categories', []))}")
    print(f"置信度: {article.get('confidence', 0)}")
    print(f"理由: {article.get('reason', '')}")
    print()
```

运行测试:
```bash
python3 scripts/test_filter.py
```

---

## 4. 运行时参数配置

### 4.1 src/auto_push.py

```python
# RSS 源配置文件路径
RSS_SOURCES_CONFIG = "/root/project-wb/n8n/config/rss-sources.json"

# 数据库路径
PUSH_HISTORY_DB = "/root/project-wb/n8n/data/push_history.db"

# 获取参数
MAX_ARTICLES_PER_SOURCE = 20    # 每个源最多获取20篇
MAX_CONCURRENT_RSS = 3           # 最大并发数
RSS_FETCH_TIMEOUT = 15           # 超时时间(秒)
RSS_MAX_RETRIES = 3              # 重试次数
RSS_RETRY_DELAY = 3              # 重试间隔(秒)
REQUEST_INTERVAL = 1.0           # 请求间隔(秒)

# 历史记录保留天数
HISTORY_RETENTION_DAYS = 30

# 获取方式
USE_API4 = True                  # True: API4, False: RSS XML

# API4 配置
API4_BASE_URL = "http://www.jintiankansha.me/api3/query"
API4_USER = "your-email@example.com"
API4_TOKEN = "YOUR_API4_TOKEN"
```

### 4.2 src/llm_filter.py

```python
# 大模型 API 配置
LLM_API_URL = "http://your-llm-api-host/agent"
LLM_API_KEY = "YOUR_LLM_API_KEY"
LLM_MODEL = "kimi-k2.5-ioa"

# 筛选参数
BATCH_SIZE = 15                  # 每批处理文章数
KEYWORDS_CONFIG = "/root/project-wb/n8n/config/keywords.json"
```

### 4.3 src/wechat_pusher.py

```python
# 企业微信 Webhook
WECHAT_WEBHOOK_URL = "https://qyapi.weixin.qq.com/cgi-bin/webhook/send"
WECHAT_WEBHOOK_KEY = "YOUR_WECHAT_WEBHOOK_KEY"
```

### 4.4 参数调优指南

#### 性能优化

**提高速度** (适用于网络良好的环境):
```python
MAX_CONCURRENT_RSS = 5           # 增加并发
REQUEST_INTERVAL = 0.5           # 减少间隔
RSS_FETCH_TIMEOUT = 10           # 减少超时
```

**增强稳定性** (适用于网络不稳定的环境):
```python
MAX_CONCURRENT_RSS = 2           # 降低并发
REQUEST_INTERVAL = 2.0           # 增加间隔
RSS_MAX_RETRIES = 5              # 增加重试次数
RSS_RETRY_DELAY = 5              # 增加重试间隔
```

#### 推送频率调整

修改 crontab:

```bash
# 增加推送频率 (每3小时)
0 8,11,14,17,20 * * * cd /root/project-wb/n8n && python3 src/auto_push.py >> logs/auto_push.log 2>&1

# 减少推送频率 (仅早晚)
0 9,20 * * * cd /root/project-wb/n8n && python3 src/auto_push.py >> logs/auto_push.log 2>&1
```

---

## 5. Docker Compose 配置

### 5.1 docker-compose.yml

```yaml
version: '3'

services:
  n8n:
    image: n8nio/n8n:1.48.0
    container_name: n8n-rss
    restart: always
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=YOUR_N8N_PASSWORD
      - N8N_SECURE_COOKIE=false
      - GENERIC_TIMEZONE=Asia/Shanghai
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=postgres
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=n8n
      - DB_POSTGRESDB_USER=n8n
      - DB_POSTGRESDB_PASSWORD=n8n_password
    volumes:
      - ./n8n-data:/home/node/.n8n
    depends_on:
      - postgres

  postgres:
    image: postgres:15-alpine
    container_name: n8n-postgres
    restart: always
    environment:
      - POSTGRES_USER=n8n
      - POSTGRES_PASSWORD=n8n_password
      - POSTGRES_DB=n8n
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

  mock-rss-server:
    build:
      context: .
      dockerfile: Dockerfile.mock
    container_name: mock-rss-server
    restart: always
    ports:
      - "5001:5000"
    volumes:
      - ./config:/app/config:ro
```

### 5.2 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `N8N_BASIC_AUTH_USER` | n8n 登录用户名 | admin |
| `N8N_BASIC_AUTH_PASSWORD` | n8n 登录密码 | YOUR_N8N_PASSWORD |
| `N8N_SECURE_COOKIE` | HTTPS 强制 | false |
| `GENERIC_TIMEZONE` | 时区设置 | Asia/Shanghai |
| `DB_TYPE` | 数据库类型 | postgresdb |

---

## 6. 配置最佳实践

### 6.1 配置文件管理

```bash
# 配置文件版本控制
config/
├── rss-sources.json        # 生产配置
├── rss-sources.dev.json    # 开发配置
├── rss-sources.test.json   # 测试配置
└── keywords.json           # 筛选规则
```

### 6.2 敏感信息分离

**不推荐** (硬编码):
```python
API4_TOKEN = "YOUR_API4_TOKEN"
```

**推荐** (环境变量):
```python
import os
API4_TOKEN = os.getenv('API4_TOKEN', 'default_token')
```

### 6.3 配置验证

创建 `scripts/validate_config.py`:

```python
#!/usr/bin/env python3
import json
import sys

def validate_rss_sources(config_path):
    """验证 RSS 源配置"""
    try:
        with open(config_path, 'r') as f:
            data = json.load(f)
        
        sources = data.get('sources', [])
        if not sources:
            print("❌ 错误: 没有配置任何 RSS 源")
            return False
        
        for i, source in enumerate(sources):
            if not source.get('name'):
                print(f"❌ 错误: 第 {i+1} 个源缺少 name 字段")
                return False
            if not source.get('slug'):
                print(f"❌ 错误: 源 '{source['name']}' 缺少 slug 字段")
                return False
            if 'enabled' not in source:
                print(f"⚠️  警告: 源 '{source['name']}' 缺少 enabled 字段,默认为 false")
        
        enabled_count = sum(1 for s in sources if s.get('enabled', False))
        print(f"✅ RSS 源配置有效: 共 {len(sources)} 个源, {enabled_count} 个已启用")
        return True
        
    except json.JSONDecodeError as e:
        print(f"❌ 错误: JSON 格式无效 - {e}")
        return False
    except Exception as e:
        print(f"❌ 错误: {e}")
        return False

def validate_keywords(config_path):
    """验证关键词配置"""
    try:
        with open(config_path, 'r') as f:
            data = json.load(f)
        
        categories = data.get('categories', [])
        if not categories:
            print("❌ 错误: 没有配置任何分类")
            return False
        
        for cat in categories:
            if not cat.get('name'):
                print(f"❌ 错误: 分类缺少 name 字段")
                return False
            if not cat.get('keywords') or len(cat['keywords']) == 0:
                print(f"❌ 错误: 分类 '{cat['name']}' 没有关键词")
                return False
        
        threshold = data.get('threshold', 0.7)
        if not (0.0 <= threshold <= 1.0):
            print(f"⚠️  警告: 置信度阈值 {threshold} 超出范围 [0.0, 1.0]")
        
        print(f"✅ 关键词配置有效: 共 {len(categories)} 个分类, 阈值 {threshold}")
        return True
        
    except Exception as e:
        print(f"❌ 错误: {e}")
        return False

if __name__ == '__main__':
    rss_ok = validate_rss_sources('/root/project-wb/n8n/config/rss-sources.json')
    keywords_ok = validate_keywords('/root/project-wb/n8n/config/keywords.json')
    
    if rss_ok and keywords_ok:
        print("\n✅ 所有配置验证通过")
        sys.exit(0)
    else:
        print("\n❌ 配置验证失败")
        sys.exit(1)
```

运行验证:
```bash
python3 scripts/validate_config.py
```

---

## 7. 常见配置问题

### 7.1 问题: 无法获取文章

**可能原因**:
- slug 错误
- API 认证失败
- 网络连接问题

**解决方法**:
```bash
# 测试单个 slug
curl -X GET "http://www.jintiankansha.me/api3/query/get_topics_by_one_column" \
  -G \
  --data-urlencode "user=YOUR_EMAIL" \
  --data-urlencode "token=YOUR_TOKEN" \
  --data-urlencode "slug=ai-frontiers"
```

### 7.2 问题: AI 筛选不准确

**可能原因**:
- 关键词配置不合理
- 置信度阈值过低/过高

**解决方法**:
1. 查看筛选日志,分析误判文章
2. 调整关键词配置
3. 调整置信度阈值

### 7.3 问题: 推送消息格式错误

**可能原因**:
- Markdown 格式错误
- 特殊字符未转义

**解决方法**:
检查 `src/wechat_pusher.py` 的格式化函数。

---

**文档版本**: v1.0  
**创建时间**: 2026-02-23  
**维护者**: AI智能筛选项目组
